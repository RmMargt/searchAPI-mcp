# Core MCP dependencies
httpx>=0.24.0
fastmcp>=0.1.0
python-dotenv>=1.0.0

# Configuration management
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Async support (included with httpx but explicit for clarity)
anyio>=3.0.0
